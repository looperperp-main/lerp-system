<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Pega o nome da aplicação do application.yml/.properties -->
    <springProperty scope="context" name="springAppName" source="spring.application.name"/>

    <!-- Appender para o Console (Padrão) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender para enviar para o Logstash via TCP -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <!-- O endereço do Logstash definido no docker-compose -->
        <destination>localhost:5000</destination>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app_name":"${springAppName}"}</customFields>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <!-- Logstash só fora dos testes: em CI/local de teste não há sink em localhost:5000
             (evita stacktrace de "Connection refused" e o backoff de ~30s de reconexão). -->
        <springProfile name="!test">
            <appender-ref ref="LOGSTASH" />
        </springProfile>
    </root>
</configuration>