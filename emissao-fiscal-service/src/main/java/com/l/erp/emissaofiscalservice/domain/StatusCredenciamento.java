package com.l.erp.emissaofiscalservice.domain;

/** spec §3 item 11 — credenciamento do tenant como emissor na SEFAZ, por UF × modelo. */
public enum StatusCredenciamento {
    PENDENTE,
    CREDENCIADO,
    BLOQUEADO
}
