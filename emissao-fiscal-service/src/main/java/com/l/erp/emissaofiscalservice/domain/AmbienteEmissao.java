package com.l.erp.emissaofiscalservice.domain;

/** spec §3 item 12 — homologação é o gate obrigatório antes de promover pra produção (§5). */
public enum AmbienteEmissao {
    HOMOLOGACAO,
    PRODUCAO
}
